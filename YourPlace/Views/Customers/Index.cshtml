@model YourPlace.ViewModels.AllCustomersViewModel
@{
    ViewBag.Title = "Index";
}

<h2>Customers</h2>

<ul>
    @foreach (var customer in Model.Customers)
    {
        <li>@Html.ActionLink(customer.Email, "Details", "Customers", new { id = customer.Id }, null) </li>
    }
</ul>

